# schema of the tool custom_bubble_chart_block


{
  "name": "custom_bubble_chart_block",
  "description": "Creates a custom bubble chart to visualize three quantitative dimensions simultaneously. Each bubble's horizontal position (x), vertical position (y), and size (radius) represents different metrics. Useful for showing relationships between three variables, such as 'revenue vs profit vs company size' or 'response time vs throughput vs error rate'.",
  "input_schema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "The title of the chart"
      },
      "x_axis_label": {
        "type": "string",
        "description": "Label describing what the horizontal axis represents (e.g., 'Response Time (ms)', 'Revenue ($M)')"
      },
      "y_axis_label": {
        "type": "string",
        "description": "Label describing what the vertical axis represents (e.g., 'Throughput (req/s)', 'Profit Margin (%)')"
      },
      "size_label": {
        "type": "string",
        "description": "Label describing what the bubble size represents (e.g., 'Number of Users', 'Data Volume (GB)', 'Error Count'). This helps users understand the third dimension."
      },
      "x_unit": {
        "type": "string",
        "description": "Optional unit for X-axis values (e.g., 'ms', 'GB', '%', '$')"
      },
      "y_unit": {
        "type": "string",
        "description": "Optional unit for Y-axis values (e.g., 'requests/sec', 'MB', '%')"
      },
      "size_unit": {
        "type": "string",
        "description": "Optional unit for the size dimension (e.g., 'users', 'GB', 'errors')"
      },
      "datasets": {
        "type": "array",
        "description": "One or more sets of bubbles. Multiple datasets allow grouping bubbles by category (e.g., different regions, product lines, or server types), with each dataset shown in a different color.",
        "items": {
          "type": "object",
          "required": [
            "label",
            "data"
          ],
          "properties": {
            "label": {
              "type": "string",
              "description": "Name for this dataset/category (e.g., 'Production Servers', 'US Region', 'Premium Tier'). Appears in the legend."
            },
            "data": {
              "type": "array",
              "description": "Individual bubble data points",
              "items": {
                "type": "object",
                "required": [
                  "x",
                  "y",
                  "r"
                ],
                "properties": {
                  "x": {
                    "type": "number",
                    "description": "Horizontal position value (mapped to x-axis scale)"
                  },
                  "y": {
                    "type": "number",
                    "description": "Vertical position value (mapped to y-axis scale)"
                  },
                  "r": {
                    "type": "number",
                    "description": "Size value (will be scaled proportionally to other bubbles). Larger values create larger bubbles. Use the actual metric value, not pixel radius - the chart will handle the visual scaling."
                  },
                  "label": {
                    "type": "string",
                    "description": "Label identifying this specific bubble (e.g., 'Server-A', 'Product X', 'Tokyo'). Shown in tooltips and can be displayed on the chart."
                  },
                  "color": {
                    "type": "string",
                    "description": "Optional hex color for this specific bubble (overrides dataset color)"
                  }
                }
              }
            },
            "color": {
              "type": "string",
              "description": "Default hex color for all bubbles in this dataset (e.g., '#3B82F6')"
            }
          }
        }
      }
    },
    "required": [
      "title",
      "datasets",
      "x_axis_label",
      "y_axis_label",
      "size_label"
    ]
  }
}
