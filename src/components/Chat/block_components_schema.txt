# schema of the tool custom_bar_chart_block

{
  "name": "custom_bar_chart_block",
  "description": "Creates a custom bar chart to compare values across categories (x-axis). Supports multiple data series that can be displayed grouped (side-by-side) or stacked (vertically).",
  "input_schema": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "The title of the chart"
      },
      "unit": {
        "type": "string",
        "description": "Unit of measurement for all values in the chart (e.g., 'requests/sec', 'GB', 'ms', '%'). This unit applies to all datasets since they share the same Y-axis scale."
      },
      "stacked": {
        "type": "boolean",
        "description": "How to display multiple data series at each category:\n- false (default): Grouped - bars from different series are shown side-by-side at each category position\n- true: Stacked - bars from different series are stacked vertically at each category position\n\nExample with series ['Server1', 'Server2'] and categories ['cpu1', 'cpu2']:\n- Grouped: At position 'cpu1' you see 2 bars side-by-side (Server1's value, Server2's value)\n- Stacked: At position 'cpu1' you see 1 bar with Server2 stacked on top of Server1",
        "default": false
      },
      "datasets": {
        "type": "array",
        "description": "One or more data series to display. Each dataset represents a different series that will be shown across all categories. The 'label' field of each data item determines which category (x-axis position) it belongs to.",
        "items": {
          "type": "object",
          "required": [
            "label",
            "data"
          ],
          "properties": {
            "label": {
              "type": "string",
              "description": "The name of this data series (e.g., 'Server1', 'Q1 Sales', 'Team A'). This appears in the chart legend and identifies which series each bar belongs to."
            },
            "data": {
              "type": "array",
              "description": "Data points for this series. Each data point belongs to a category (determined by its 'label' field). All series should have data points for the same set of categories.",
              "items": {
                "type": "object",
                "required": [
                  "label",
                  "v"
                ],
                "properties": {
                  "label": {
                    "type": "string",
                    "description": "The category name for this data point (e.g., 'cpu1', 'cpu2', 'ProductA'). This determines the x-axis position. Data points from different series with the same category label will be grouped/stacked together at this x-axis position."
                  },
                  "v": {
                    "type": "number",
                    "description": "The numeric value for this data point (determines bar height)"
                  },
                  "color": {
                    "type": "string",
                    "description": "Optional hex color for this specific bar (overrides the series color)"
                  }
                }
              }
            },
            "color": {
              "type": "string",
              "description": "Default hex color for all bars in this data series"
            }
          }
        }
      },
      "x_axis_label": {
        "type": "string",
        "description": "Label for the x-axis (category axis)"
      },
      "y_axis_label": {
        "type": "string",
        "description": "Label for the y-axis (value axis)"
      }
    },
    "required": [
      "title",
      "datasets"
    ]
  }
}
